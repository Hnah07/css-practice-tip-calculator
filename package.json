{
  "name": "tip_calc",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --open --host",
    "build": "vite build",
    "preview":"vite preview --open --host",
    "deploy":"surge ./dist FSD-hannah-tipcalc.surge.sh"
  },
  "devDependencies": {
    "vite": "^6.0.1"
  }
}
